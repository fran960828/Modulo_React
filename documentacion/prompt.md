rol: Experto en React con typescript. consulta: Haz una documentación para una persona principiante con una explicación en forma de comentario al inicio y tras la explicación emplea un ejemplo sencillo para que quede más claro de los siguientes conceptos:

- Como se forma un componente estático en react: uso de funciones con la primera letra en mayuscula.
- Como usar un componente ya creado utilizando esta sintaxis: <Componente/>
- Como renderizar el arbol de componentes creados con react en el html con createRoot(elementodelDom).render(<App/>)
- Como introducir contenido dinámico en un componente con llaves {}.
- Como introducir imagenes de forma dinamica para no tener problemas en el despliegue.import reactImage from src/assets/imagen.pnt
- Como Hacer componentes reutilizables a tráves del uso de props.
- Como importar estilos css a cada componente creando un archivo css con el mismo nombre del componente e importandolo al archivo tsx
- Como usar children como React.ReactNode para introducir información entre las dos etiquetas del componente <Componente>Informacion</Componente/>
- Uso de eventos como onclick en componentes y declaración de funciones dentro del componente que se aplicarán al evento.
- Uso de eventos como onclick en componentes y inyeccion de función a través del prop para poder tener acceso a otros contenidos fuera del ámbito del componente.
- Uso de una arrow functions dentro del evento (en este caso onClick) de este modo podemos pasar parametros a la función para que dependiendo del boton tenga un comportamiento distinto.
- uso de useState estableciendo el valor de una variable y el método para cambiarla de forma interactiva a través de un evento.
- uso de condicional ternario para determinar si muestra o no un determinado contenido en función del valor de la variable de useState.
- uso del condicional ternario para cambiar el nombre de la clase de forma que le aplicamos unos estilos determinados a un elemento elegido.

Especificaciones:-La documentación debe contener la explicación detallada de todo lo necesario para el uso de los conceptos a nivel profesional-Los ejemplos deben estar explicados con comentarios sobre lo que hacen en cada paso -El formato de entrega será markdown. Verificación:Revisa el contenido de la consulta para obtener el resultado deseado, recuerda que lo más importante es que los ejemplos estén bien explicados , tomate el tiempo necesario para obtener el mejor resultado.

genera el markdown de la documentación para descargar pero no te dejes nada de lo que has desarrollado en el primer prompt.

genera un serie de ejercicios para practicar todos estos conceptos, estos ejercicios deben diferir de los que has puesto de ejemplo y aumentar progresivamente de dificultad hasta el punto de alcanzar un uso profesional y fluido. Genera el número de ejercicios que consideres necesario para alcanzar un buen nivel de dominio
